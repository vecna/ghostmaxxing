# playwright-report

This folder contains generated Playwright HTML reports.

It is produced by Playwright test runs such as `npm run test:e2e` and is useful for inspecting browser test failures locally.

The report HTML is excluded from code2prompt because it is generated output, not maintained source.
