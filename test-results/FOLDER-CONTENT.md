# test-results

This folder contains generated Playwright test-result metadata and artifacts.

It is created by Playwright during browser test runs and may include run state, traces, screenshots, videos, or other failure artifacts depending on the test configuration and the last run.

The contents are excluded from code2prompt because they are ephemeral test output.
