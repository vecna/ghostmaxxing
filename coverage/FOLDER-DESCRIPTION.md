# coverage

This folder contains generated Vitest coverage output.

It is produced by `npm run test:coverage` and includes HTML reports, coverage JSON, `lcov.info`, and static report assets. The README coverage badge links here for local inspection.

Coverage output is excluded from code2prompt because it is generated test-report data. The chatbot should infer that coverage exists from this summary and from the package scripts.
