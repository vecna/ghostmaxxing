# docs

This folder contains generated JSDoc documentation for the project.

The files are generated from the source comments and documentation configuration with `npm run docs`. The root README and `JSDOC_index.md` provide the useful human summary; the generated HTML, search indexes, page assets, and copied source views are not uploaded to the chatbot.

Regenerate this folder with `npm run docs:rebuild` after source documentation changes.
